{
  "name": "spa-institucional-api",
  "version": "0.0.0",
  "scripts": {
    "start": "json-server --no-cors --watch products.json",
    "build": "ng build"
  },
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
